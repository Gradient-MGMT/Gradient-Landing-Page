version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site; no build step required."
  artifacts:
    baseDirectory: .
    files:
      - '*.html'
      - 'robots.txt'
      - 'sitemap.xml'
      - '*.css'
      - '*.js'
      - 'assets/**/*'
  cache:
    paths: []
